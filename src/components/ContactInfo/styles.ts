import styled from 'styled-components';
import tw from 'tailwind.macro';

export const ContactInfoItem = styled.div`
  ${tw`w-full`};
`;

export const Contact = styled.section`
  ${tw`bg-gray-100 border-b`}
`;